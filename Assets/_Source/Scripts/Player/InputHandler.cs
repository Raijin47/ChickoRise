using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputHandler : MonoBehaviour
{
    //public event Action<float> OnInputHorizontal;
    //public event Action<float> OnInputVertical;

    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.LeftArrow)) OnInputHorizontal?.Invoke(Vector2.left);
    //    if (Input.GetKeyDown(KeyCode.RightArrow)) OnInputChange?.Invoke(Vector2.right);
    //    if (Input.GetKeyDown(KeyCode.UpArrow)) OnInputChange?.Invoke(Vector2.up);
    //    if (Input.GetKeyDown(KeyCode.DownArrow)) OnInputChange?.Invoke(Vector2.down);
    //}
}